import { FilterCountry, InputBar } from "./"

export const SearchBar = () => {
     return (
          <div className="search-section">
               <InputBar />
               <FilterCountry />
          </div>
     )
}
